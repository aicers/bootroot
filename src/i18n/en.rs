use super::Strings;

pub(super) static STRINGS: Strings = Strings {
    infra_up_completed: "bootroot infra up: completed",
    infra_readiness_summary: "bootroot infra up: readiness summary",
    readiness_entry_with_health: "- {service}: {status} (health: {health})",
    readiness_entry_without_health: "- {service}: {status}",
    infra_unhealthy: "Infrastructure not healthy: {failures}",
    monitoring_up_completed: "bootroot monitoring up: completed",
    monitoring_readiness_summary: "bootroot monitoring up: readiness summary",
    monitoring_unhealthy: "Monitoring not healthy: {failures}",
    monitoring_status_title: "bootroot monitoring status: summary",
    monitoring_status_profile: "- profile: {value}",
    monitoring_status_section_services: "- services:",
    status_entry_with_health: "  - {service}: {status} (health: {health})",
    status_entry_without_health: "  - {service}: {status}",
    monitoring_status_grafana_url: "- grafana: {value}",
    monitoring_status_grafana_admin_password: "- grafana admin password: {value}",
    monitoring_status_value_set: "set",
    monitoring_status_value_default: "default (admin)",
    monitoring_status_value_unknown: "unknown",
    monitoring_status_no_services: "bootroot monitoring status: no monitoring services found",
    monitoring_down_completed: "bootroot monitoring down: completed",
    monitoring_down_reset_grafana: "bootroot monitoring down: grafana admin password reset",
    monitoring_down_reset_grafana_skipped: "bootroot monitoring down: grafana reset skipped",
    monitoring_up_already_running: "bootroot monitoring up: already running (run `bootroot monitoring down` first if you want to restart)",
    error_service_no_container: "Service has no running container: {service}",
    init_failed_rollback: "bootroot init: failed, attempting rollback",
    prompt_openbao_root_token: "OpenBao root token: ",
    error_openbao_root_token_required: "OpenBao root token is required; pass --root-token, --root-token-file, or the OPENBAO_ROOT_TOKEN env var",
    warning_openbao_unseal_from_file: "WARNING: Auto-unseal from file is for dev/test only and unsafe for production.",
    warning_openbao_sealed_non_interactive: "WARNING: OpenBao is sealed but stdin is not interactive. Supply --openbao-unseal-from-file or run `bootroot openbao save-unseal-keys` first.",
    error_openbao_audit_setup_failed: "OpenBao audit backend setup failed",
    warning_db_password_rotation_skipped: "WARNING: DB password rotation skipped; run `bootroot rotate db` later to rotate manually.",
    warning_secret_id_ttl_exceeds_recommended: "WARNING: --secret-id-ttl ({value}) exceeds the recommended threshold ({threshold}). Consider a shorter TTL for tighter credential rotation.",
    error_secret_id_ttl_exceeds_max: "--secret-id-ttl ({value}) exceeds the maximum allowed value ({max})",
    error_secret_id_ttl_invalid: "Invalid --secret-id-ttl value: {value}. Use a duration like \"24h\", \"30m\", or \"3600s\".",
    error_rn_cidrs_invalid: "Invalid --rn-cidrs value: {value}. Use CIDR notation (e.g. \"10.0.0.0/24\", \"fd00::/64\").",
    error_rn_cidrs_clear_conflict: "\"clear\" cannot be combined with other --rn-cidrs values",
    error_rn_cidrs_clear_on_add: "\"clear\" is only valid for service update; omit --rn-cidrs to leave CIDR binding unset",
    prompt_openbao_unseal_from_file_confirm: "Proceed with auto-unseal from file {value}? [y/N]: ",
    error_openbao_unseal_file_empty: "Unseal key file is empty: {value}",
    openbao_unseal_source_in_memory: "the unseal keys held by this init run",
    openbao_unseal_source_prompt: "the interactive unseal key prompt",
    init_openbao_tls_recreate: "bootroot init: recreating OpenBao so it loads the TLS listener configuration",
    init_registrar_internal_provisioning: "bootroot init: provisioning the bootroot-internal registrar credential",
    init_registrar_internal_ready: "bootroot init: the bootroot-internal registrar credential is ready; start its agent with: {invocation}",
    init_openbao_tls_verified: "bootroot init: OpenBao is serving TLS at {url}",
    init_openbao_already_unsealed: "bootroot init: OpenBao already reports itself unsealed; no unseal keys submitted",
    init_openbao_unseal_from_source: "bootroot init: unsealing OpenBao with {source}",
    error_openbao_tls_probe_failed: "OpenBao did not answer an API request over TLS at {url}; the listener may still be serving plaintext",
    error_openbao_unseal_source_unavailable: "OpenBao must be unsealed after the TLS listener restart, but no unseal keys are available. Pass --openbao-unseal-from-file <path>, save them to {path} with `bootroot openbao save-unseal-keys`, or run init on a terminal to enter them interactively",
    error_openbao_unseal_source_still_sealed: "OpenBao remains sealed after applying the unseal keys from {source}",
    prompt_unseal_threshold: "Unseal key threshold (t): ",
    prompt_unseal_key: "Unseal key {index}/{count}: ",
    prompt_stepca_password: "step-ca password",
    prompt_http_hmac: "HTTP-01 responder HMAC",
    prompt_db_admin_dsn: "PostgreSQL admin DSN",
    prompt_db_dsn: "PostgreSQL DSN",
    prompt_db_user: "PostgreSQL user",
    prompt_db_password: "PostgreSQL password",
    prompt_db_name: "PostgreSQL database name",
    error_invalid_unseal_threshold: "Invalid unseal threshold value",
    error_eab_requires_both: "EAB requires both kid and hmac",
    error_openbao_sealed: "OpenBao remains sealed after applying unseal keys",
    error_invalid_db_dsn: "Invalid PostgreSQL DSN",
    error_db_host_not_single_host: "PostgreSQL host violates single-host guardrail: {host}",
    error_db_host_compose_runtime: "PostgreSQL host '{host}' is not reachable from step-ca container runtime; use '{expected}' in db-dsn/db-admin-dsn",
    error_postgres_port_binding_unsafe: "PostgreSQL port must be bound to localhost only (for example, 127.0.0.1:5432:5432)",
    error_service_port_binding_unsafe: "{service} port must be bound to localhost only (for example, 127.0.0.1:<port>:<port>)",
    error_db_check_failed: "DB connectivity check failed",
    error_db_auth_failed: "DB authentication check failed",
    error_db_type_unsupported: "DB check requires postgresql db.type",
    error_db_provision_conflict: "db-provision cannot be used with db-dsn",
    error_invalid_db_identifier: "Invalid DB identifier: {value}",
    prompt_eab_register_now: "Register EAB now? [y/N]: ",
    eab_prompt_instructions: "Generate EAB credentials in your ACME CA (EAB-capable commercial offering) and paste the values below.",
    prompt_eab_kid: "EAB kid: ",
    prompt_eab_hmac: "EAB hmac: ",
    prompt_confirm_db_provision: "Provision PostgreSQL role/database? [y/N]: ",
    error_responder_check_failed: "HTTP-01 responder check failed",
    error_responder_unreachable: "The HTTP-01 responder at {endpoint} never accepted a connection within {elapsed}s. Last transport error: {error}. Check that the responder container is running and that its admin port is published; raise --responder-ready-timeout-secs if it simply needs longer to start.",
    error_responder_rejected: "The HTTP-01 responder at {endpoint} answered {status}: {body}. The responder is reachable, so check the responder URL and the HMAC secret rather than waiting longer.",
    error_responder_ready_timeout_invalid: "--responder-ready-timeout-secs must be greater than 0",
    error_state_missing: "state.json not found (run `bootroot init` first)",
    error_service_duplicate: "Service already exists: {value}",
    error_service_agent_config_conflict: "Agent config {path} is already used by service {service}. Each distinct local-file service needs its own agent.toml: the [openbao] section holds a single AppRole identity, so two services sharing one config would overwrite each other's credentials",
    error_service_agent_config_stale_profile: "Agent config {path} still contains a bootroot-managed profile for service {service} (likely left by `service remove` without --strip-config/--delete-artifacts). The agent fast-polls every profile in a config under its single [openbao] AppRole identity, so the stale service would run under this service's credentials. Delete the `# BEGIN/END bootroot managed profile: {service}` block (or use a separate agent.toml) and retry",
    error_service_secret_id_path_requires_local_file: "--secret-id-path is only honoured for local-file delivery; remote-bootstrap services record the control-host secret_id path in the bootstrap artifact, so it cannot be relocated here",
    error_service_secret_id_path_not_absolute: "--secret-id-path must be an absolute path: {value}",
    error_service_secret_id_path_role_id_collision: "--secret-id-path must not end in `role_id`: the sibling `role_id` file is derived as <dir>/role_id and would collide with secret_id at {value}",
    error_service_secret_id_path_inside_secrets_dir: "--secret-id-path must resolve outside the root-owned secrets tree {secrets_dir}: a path inside it ({value}) cannot be traversed by the non-root agent",
    error_service_secret_id_path_parent_missing: "--secret-id-path parent directory does not exist: {value} (provision it, owned by the agent account, before running `service add`)",
    error_service_policy_mismatch: "To change the secret_id policy for an existing service, use `bootroot service update`",
    error_service_not_found: "Service not found: {value}",
    error_service_instance_id_required: "instance_id is required for all services",
    error_value_required: "Value is required",
    error_service_name_invalid: "service_name must be a DNS label (letters, digits, hyphens only; max 63 chars)",
    error_service_name_reserved: "service_name must not start with `{value}`: that prefix is reserved for bootroot's own certificate identities",
    error_registration_id_invalid: "registration_id must be lowercase letters, digits and hyphens, starting and ending with a letter or digit (max 131 chars)",
    error_hostname_invalid: "hostname must be a DNS label (letters, digits, hyphens only; max 63 chars)",
    error_domain_invalid: "domain must be a DNS name with dot-separated labels (letters, digits, hyphens only)",
    error_instance_id_invalid: "instance_id must be numeric",
    error_path_not_found: "Path not found: {value}",
    error_parent_not_found: "Parent directory not found: {value}",
    error_operation_cancelled: "Operation cancelled",
    error_infra_failed: "bootroot infra up failed",
    error_monitoring_failed: "bootroot monitoring failed",
    error_init_failed: "bootroot init failed",
    error_status_failed: "bootroot status failed",
    error_service_add_failed: "bootroot service add failed",
    error_service_info_failed: "bootroot service info failed",
    error_service_remove_failed: "bootroot service remove failed",
    error_verify_failed: "bootroot verify failed",
    error_rotate_failed: "bootroot rotate failed",
    error_details: "details: {value}",
    error_runtime_init_failed: "Failed to initialize async runtime for {command}",
    error_prompt_write_failed: "Failed to write prompt",
    error_prompt_flush_failed: "Failed to flush prompt",
    error_prompt_read_failed: "Failed to read prompt input",
    error_prompt_eof: "no input available (stdin reached EOF / not a terminal)",
    error_prompt_error_write_failed: "Failed to write prompt error",
    error_file_missing: "Required file missing: {value}",
    error_read_file_failed: "Failed to read {value}",
    error_write_file_failed: "Failed to write {value}",
    error_read_dir_failed: "Failed to read directory: {value}",
    error_read_dir_entry_failed: "Failed to read directory entry",
    error_remove_file_failed: "Failed to remove {value}",
    error_restore_file_failed: "Failed to restore {value}",
    error_resolve_path_failed: "Failed to resolve {value}",
    error_generate_secret_failed: "Failed to generate random secret",
    error_db_auth_task_failed: "DB auth check task failed",
    error_db_provision_task_failed: "DB provisioning task failed",
    error_stepca_password_missing: "step-ca password file not found at {value}",
    error_openbao_client_create_failed: "Failed to create OpenBao client",
    error_openbao_health_check_failed: "OpenBao health check failed",
    error_openbao_init_status_failed: "OpenBao init status check failed",
    error_openbao_init_failed: "OpenBao init request failed",
    error_openbao_seal_status_failed: "OpenBao seal status request failed",
    error_openbao_unseal_failed: "OpenBao unseal request failed",
    error_openbao_kv_mount_failed: "OpenBao KV mount check failed",
    error_openbao_kv_mount_status_failed: "OpenBao KV mount status check failed",
    error_openbao_approle_auth_failed: "OpenBao AppRole auth setup failed",
    error_openbao_policy_exists_failed: "OpenBao policy lookup failed",
    error_openbao_policy_write_failed: "OpenBao policy write failed",
    error_openbao_approle_exists_failed: "OpenBao AppRole lookup failed",
    error_openbao_approle_create_failed: "OpenBao AppRole create failed",
    error_openbao_role_id_failed: "OpenBao AppRole role_id read failed",
    error_openbao_secret_id_failed: "OpenBao AppRole secret_id create failed",
    error_openbao_approle_login_failed: "OpenBao AppRole login failed",
    error_openbao_kv_exists_failed: "OpenBao KV secret lookup failed",
    error_openbao_kv_write_failed: "OpenBao KV secret write failed",
    error_openbao_kv_read_failed: "OpenBao KV secret read failed",
    error_openbao_kv_delete_failed: "OpenBao KV secret delete failed",
    error_openbao_role_output_missing: "OpenBao AppRole output missing: {value}",
    error_parse_container_env_failed: "Failed to parse container environment variables",
    error_parse_container_mounts_failed: "Failed to parse container mounts",
    error_command_run_failed: "Failed to run {value}",
    error_command_failed_status: "{value} failed with status: {status}",
    error_docker_compose_failed: "docker compose failed: {value}",
    error_docker_command_failed: "docker command failed: {value}",
    error_bootroot_agent_run_failed: "Failed to run bootroot-agent",
    error_bootroot_agent_not_found: "bootroot-agent binary not found. Tried: {candidates}",
    error_secrets_dir_resolve_failed: "Failed to resolve secrets dir",
    error_parse_ca_json_failed: "Failed to parse ca.json",
    error_serialize_ca_json_failed: "Failed to serialize ca.json",
    error_rendered_file_timeout: "Timed out waiting for OpenBao Agent to render {value}",
    error_ca_json_db_missing: "ca.json missing db.dataSource field",
    error_ca_cert_missing: "CA certificate not found: {value}",
    error_ca_cert_parse_failed: "Failed to parse CA certificate: {value}",
    error_ca_trust_missing: "OpenBao CA trust data missing key: {value}",
    error_ca_trust_invalid: "OpenBao CA trust data is invalid",
    error_ca_trust_empty: "OpenBao CA trust data is empty",
    error_parse_state_failed: "Failed to parse state.json",
    error_serialize_state_failed: "Failed to serialize state.json",
    prompt_registration_id: "Registration ID",
    prompt_service_name: "Service name",
    prompt_hostname: "Hostname",
    prompt_domain: "Domain",
    prompt_agent_config: "bootroot-agent config path",
    prompt_cert_path: "Certificate path",
    prompt_key_path: "Key path",
    prompt_instance_id: "Instance ID",
    prompt_confirm_overwrite_password: "Overwrite password.txt? [y/N]: ",
    prompt_confirm_overwrite_ca_json: "Overwrite ca.json? [y/N]: ",
    prompt_confirm_overwrite_state: "Overwrite state.json? [y/N]: ",
    prompt_rotate_stepca_password: "Rotate step-ca key password? [y/N]",
    prompt_rotate_db: "Rotate step-ca database password? [y/N]",
    prompt_rotate_responder_hmac: "Rotate HTTP-01 responder HMAC? [y/N]",
    prompt_rotate_approle_secret_id: "Rotate AppRole secret_id for {service_name}? [y/N]",
    prompt_rotate_all_approle_secret_ids: "Rotate AppRole secret_ids for all {count} registered service(s)? [y/N]",
    prompt_rotate_infra_approle_secret_id: "Rotate infra AppRole secret_id for {role_name}? [y/N]",
    init_plan_title: "bootroot init: plan",
    init_plan_overwrite_password: "- password.txt: overwrite",
    init_plan_overwrite_ca_json: "- ca.json: overwrite",
    init_plan_overwrite_state: "- state.json: overwrite",
    service_add_summary: "bootroot service add: summary",
    service_add_plan_title: "bootroot service add: plan",
    service_info_summary: "bootroot service info: summary",
    service_summary_registration_id: "- registration id: {value}",
    service_summary_kind: "- service name: {value}",
    service_summary_hostname: "- hostname: {value}",
    service_summary_domain: "- domain: {value}",
    service_summary_delivery_mode: "- delivery mode: {value}",
    service_summary_instance_id: "- instance_id: {value}",
    service_summary_notes: "- notes: {value}",
    service_summary_post_renew_hook: "- post-renew hook: {value}",
    service_summary_policy: "- policy: {value}",
    service_summary_approle: "- AppRole: {value}",
    service_summary_secret_path: "- secret_id path: {value}",
    service_summary_openbao_path: "- OpenBao path: bootroot/services/{value}",
    service_summary_dns_alias_registered: "- HTTP-01 DNS aliases registered: {count}",
    service_summary_dns_alias_skipped: "- HTTP-01 DNS aliases registered: 0 (registration skipped; see the warning above)",
    service_summary_auto_applied_agent_config: "- auto-applied bootroot-agent config: {value}",
    service_summary_auto_applied_eab_file: "- auto-provisioned EAB file (present only when EAB is configured; pass its path via --eab-file): {value}",
    service_scope_bootroot_managed: "Bootroot-managed:",
    service_scope_operator_required: "Operator-managed (required):",
    service_scope_operator_recommended: "Operator-managed (recommended):",
    service_scope_operator_optional: "Operator-managed (optional):",
    service_summary_remote_bootstrap_file: "- remote bootstrap file (machine-readable artifact for automation): {value}",
    service_summary_remote_run_command: "- remote run command template: {value}",
    service_summary_remote_handoff_title: "- remote handoff order:",
    service_summary_remote_handoff_service_host: "  1. Copy bootstrap.json and role_id to the service host, then replace <REMOTE_ARTIFACT_PATH> and run: {value}",
    service_summary_remote_handoff_service_host_no_wrap: "  1. Copy bootstrap.json, role_id, and secret_id to the service host, then replace <REMOTE_ARTIFACT_PATH> and run: {value}",
    service_summary_remote_placeholder_warning: concat!(
        "  - warning: the printed command template and matching bootstrap.json fields use ",
        "localhost placeholders for `--agent-server` and `--agent-responder-url`; ",
        "replace them with remote-reachable step-ca/responder endpoints before ",
        "running on a separate service machine."
    ),
    service_summary_remote_handoff_status_check: "  2. Check status on the step-ca host: {value}",
    service_summary_agent_config: "- agent config: {value}",
    service_summary_cert_path: "- cert path: {value}",
    service_summary_key_path: "- key path: {value}",
    service_summary_next_steps: "next steps:",
    service_summary_preview_mode: "- preview mode: no files or state were changed",
    service_summary_preview_trust_skipped_no_token: "- trust preview unavailable: provide OpenBao runtime auth (--root-token or --auth-mode approle with AppRole credentials) in preview mode",
    service_summary_preview_trust_not_found: "- trust preview unavailable: OpenBao CA trust data (secret/bootroot/ca) was not found",
    service_summary_preview_trust_lookup_failed: "- trust preview lookup failed: {value}",
    service_summary_remote_idempotent_hint: "- existing remote-bootstrap service matched input; bootstrap artifact was refreshed",
    service_next_steps_daemon_profile: "  - Add profile for {registration_id} (service_name={service_name}, instance_id={instance_id}, hostname={hostname}, domain={domain}, cert={cert_path}, key={key_path}) to {config_path} and reload bootroot-agent.",
    service_next_steps_remote_selfheal_keep: "  - Keep bootroot-agent running on the remote host after `bootroot-remote bootstrap` (see the remote handoff order above).",
    service_next_steps_remote_selfheal_note: concat!(
        "  - No OpenBao Agent runs on the remote host: the bootroot-agent self-authenticates ",
        "and pulls trust (ca-bundle.pem + trusted_ca_sha256) and secret_id rotations via fast-poll, ",
        "so CA/trust and secret_id rotations propagate without a manual re-bootstrap or apply-secret-id."
    ),
    service_snippet_daemon_title: "daemon profile snippet:",
    service_snippet_daemon_run_title: "daemon run command (systemd ExecStart or shell; --eab-file is required for EAB rotation to apply):",
    service_snippet_trust_title: "trust settings snippet:",
    service_snippet_domain_hint: "Domain should be set to \"{value}\" in agent config.",
    verify_plan_title: "bootroot verify: plan",
    verify_summary_title: "bootroot verify: summary",
    verify_registration_id: "- registration id: {value}",
    verify_service_name: "- service name: {value}",
    verify_agent_config: "- agent config: {value}",
    verify_cert_path: "- cert path: {value}",
    verify_key_path: "- key path: {value}",
    verify_result_ok: "- result: ok",
    verify_agent_failed: "bootroot-agent failed during verification",
    verify_missing_cert: "Certificate not found at {value}",
    verify_missing_key: "Key not found at {value}",
    verify_empty_cert: "Certificate file is empty at {value}",
    verify_empty_key: "Key file is empty at {value}",
    verify_cert_parse_failed: "Failed to parse certificate",
    verify_cert_missing_san: "Certificate missing DNS SANs",
    verify_cert_san_mismatch: "Certificate SAN mismatch (expected: {expected}, actual: {actual})",
    verify_agent_config_load_failed: "Failed to load agent config at {path}: {reason}",
    verify_ca_bundle_read_failed: "Failed to read CA bundle at {path}",
    verify_ca_bundle_parse_failed: "Failed to parse CA bundle at {path}",
    verify_ca_bundle_missing_fingerprints: "CA bundle at {path} is missing trusted fingerprints: {missing}",
    verify_cert_chain_failed: "Leaf certificate at {cert_path} does not chain to CA bundle at {bundle_path}; reissue the leaf so it matches the current PKI generation.",
    status_summary_title: "bootroot status: summary",
    status_section_infra: "- infra:",
    status_section_openbao: "- OpenBao:",
    status_section_kv_paths: "- KV paths:",
    status_section_approles: "- AppRoles:",
    status_section_services: "- services:",
    status_services_none: "  - none registered",
    status_openbao_health: "  - health: {value}",
    status_openbao_sealed: "  - sealed: {value}",
    status_openbao_kv_mount: "  - kv mount ({mount}): {value}",
    status_kv_path_entry: "  - {path}: {value}",
    status_approle_entry: "  - {role}: {value}",
    status_service_delivery_mode: "  - {service} delivery mode: {value}",
    status_value_ok: "ok",
    status_value_unreachable: "unreachable",
    status_value_present: "present",
    status_value_missing: "missing",
    status_value_optional_missing: "missing (optional)",
    status_value_unknown: "unknown",
    status_value_invalid: "invalid",
    status_error_infra_unhealthy: "Infrastructure not healthy: {failures}",
    status_error_openbao_unreachable: "OpenBao is unreachable",
    status_last_secret_id_rotation: "  - last AppRole secret_id rotation success: {value}",
    status_warning_secret_id_rotation_stale: "WARNING: the last successful AppRole secret_id rotation was {age} ago (threshold: {threshold}). The scheduled rotation job may have stopped; investigate before the rotate credentials expire. Recovery: re-mint with the root token (break-glass procedure, see the operations guide).",
    status_section_registrar_audit: "- registrar audit:",
    status_audit_not_configured: "  - not configured",
    status_audit_unpaired_intents: "  - unpaired intents: {value}",
    status_audit_malformed_records: "  - malformed records: {value}",
    status_audit_retention_shortfall: "  - retention shortfall: {value}",
    status_audit_yes: "yes",
    status_audit_no: "no",
    status_warning_audit_unpaired_intents: "WARNING: registrar audit contains unpaired intents.",
    status_warning_audit_malformed_records: "WARNING: registrar audit contains malformed records.",
    status_warning_audit_retention_shortfall: "WARNING: registrar audit retention is shorter than configured.",
    status_warning_audit_scan_failed: "WARNING: registrar audit scan failed for {path}: {reason}",
    status_warning_audit_config_not_regular_file: "agent configuration path is not a regular file",
    status_error_agent_config_missing: "Agent config path does not exist: {path}",
    summary_title: "bootroot init: summary",
    summary_openbao_url: "- OpenBao URL: {value}",
    summary_kv_mount: "- KV mount: {value}",
    summary_secrets_dir: "- Secrets dir: {value}",
    summary_stepca_completed: "- step-ca init: completed",
    summary_stepca_skipped: "- step-ca init: skipped (already initialized)",
    summary_openbao_init_completed: "- OpenBao init: completed (shares={shares}, threshold={threshold})",
    summary_openbao_init_skipped: "- OpenBao init: skipped (already initialized)",
    summary_root_token: "- root token: {value}",
    summary_unseal_key: "- unseal key {index}: {value}",
    summary_stepca_password: "- step-ca password: {value}",
    summary_db_dsn: "- db dsn: {value}",
    summary_responder_hmac: "- responder hmac: {value}",
    summary_eab_kid: "- eab kid: {value}",
    summary_eab_hmac: "- eab hmac: {value}",
    summary_eab_missing: "- eab: not configured",
    rotate_summary_title: "bootroot rotate: summary",
    rotate_summary_stepca_password: "- step-ca password updated: {value}",
    rotate_summary_restart_stepca: "- step-ca: restarted",
    rotate_summary_db_dsn: "- ca.json updated: {value}",
    rotate_summary_responder_config: "- responder config updated: {value}",
    rotate_summary_reload_responder: "- responder: sent SIGHUP",
    rotate_summary_approle_secret_id: "- AppRole secret_id rotated for {service_name}: {value}",
    rotate_summary_approle_login_ok: "- AppRole login OK for {service_name}",
    rotate_summary_infra_approle_secret_id: "- infra AppRole secret_id rotated for {role_name}: {value}",
    rotate_summary_infra_agent_restarted: "- OpenBao Agent restarted: {container}",
    rotate_summary_infra_approle_login_ok: "- AppRole login OK for {role_name}",
    rotate_infra_provisioned_role: "- provisioned AppRole {role_name} with policy {policy_name} (created or refreshed)",
    rotate_infra_provisioned_role_id: "- {role_name} role_id: {value}",
    rotate_infra_provisioned_secret_id: "- {role_name} secret_id: {value} (store it securely; use it for future --infra rotations via --auth-mode approle)",
    rotate_infra_cidr_binding_kept: "- re-applied the recorded CIDR binding for {role_name}: {value} (pass --rotate-bound-cidrs to change it, --clear-rotate-bound-cidrs to remove it)",
    rotate_infra_cidr_binding_cleared: "- cleared the recorded CIDR binding for {role_name}: the operator credential and subsequent self-mints are unbound",
    error_infra_secret_id_mint_failed: "Failed to mint a secret_id for {role_name}. Infra targets require bootroot-infra-rotate-role credentials (--auth-mode approle). On deployments initialized before that role existed, run this command once with the root token to provision it.",
    error_service_secret_id_mint_failed: "Failed to mint a secret_id for service {service_name}. Service targets require bootroot-runtime-rotate-role credentials (--auth-mode approle).",
    rotate_all_no_services: "No services are registered; nothing to rotate.",
    rotate_all_target_failed: "- AppRole secret_id rotation FAILED for {service_name}: {error}",
    rotate_all_result: "- services rotated: {ok} succeeded, {failed} failed (total {total})",
    error_rotate_all_partial_failure: "AppRole secret_id rotation failed for {failed} of {total} service(s): {names}",
    rotate_summary_self_mint: "- re-minted own {role_name} secret_id (num_uses={num_uses}): {value}",
    rotate_summary_self_mint_login_ok: "- self-mint login verification OK for {role_name}",
    warning_self_mint_skipped_non_file: "WARNING: skipped the {role_name} self-mint: AppRole auth was not file-based. The self-mint step replaces the file passed via --approle-secret-id-file; with an inline or env-supplied secret_id there is no file to replace. The current credential expires at its TTL — switch the scheduled job to --approle-secret-id-file, or re-mint before expiry.",
    error_self_mint_failed: "Failed to re-mint the {role_name} credential's own secret_id (all rotation targets succeeded). The current credential stays valid until its TTL and the next run retries the self-mint.",
    error_self_mint_verify_failed: "The freshly self-minted {role_name} secret_id failed login verification; the existing credential file is left untouched. Check the recorded rotate-bound CIDRs — a binding that excludes this host would lock out the scheduled job.",
    error_rotate_bound_cidrs_requires_provisioning: "--rotate-bound-cidrs is only honored on the root-token provisioning run (--infra with root auth); re-run with the root token or drop the flag",
    error_clear_rotate_bound_cidrs_requires_provisioning: "--clear-rotate-bound-cidrs is only honored on the root-token provisioning run (--infra with root auth); re-run with the root token or drop the flag",
    error_rotate_bound_cidrs_invalid: "Invalid --rotate-bound-cidrs value: {value}. Use CIDR notation (e.g. \"10.0.0.5/32\", \"fd00::/64\").",
    rotate_summary_openbao_recovery_targets: "- OpenBao recovery rotation: {value}",
    rotate_summary_openbao_recovery_output: "- recovery credentials written: {value}",
    rotate_summary_openbao_recovery_approle_unchanged: "- AppRole + SecretID configuration unchanged",
    rotate_summary_openbao_recovery_next_steps: "- next steps: store new credentials securely, revoke old root token if still valid, and validate OpenBao health",
    prompt_rotate_openbao_recovery: "Rotate OpenBao recovery credentials ({value})? AppRole + SecretID will remain unchanged. [y/N]",
    error_openbao_recovery_target_required: "Specify at least one target: --rotate-unseal-keys and/or --rotate-root-token",
    error_openbao_recovery_unseal_keys_required: "At least {value} existing unseal keys are required for root-key rotation",
    prompt_rotate_trust_sync: "Sync CA trust data to OpenBao and update services? [y/N]",
    prompt_rotate_force_reissue: "Delete certificates for {service_name} and trigger reissuance? [y/N]",
    prompt_rotate_force_reissue_remote: "Write a reissue request for remote-bootstrap service {service_name} to OpenBao? The remote agent will apply it on its next fast-poll tick. [y/N]",
    rotate_summary_trust_sync_global: "- CA trust updated: {value}",
    rotate_summary_trust_sync_service: "- service trust synced: {value}",
    rotate_summary_force_reissue_deleted: "- {service_name}: cert/key deleted ({cert_path}, {key_path})",
    rotate_summary_force_reissue_local_signal: "- {service_name}: signaled bootroot-agent for renewal",
    rotate_summary_force_reissue_requested: "- {service_name}: reissue requested at {requested_at}",
    rotate_summary_force_reissue_will_apply: "- {service_name}: remote agent will apply within ~30s of its next fast-poll",
    rotate_summary_force_reissue_completed: "- {service_name}: bootroot-agent reported completion at {completed_at} (end-to-end latency: {elapsed})",
    rotate_summary_force_reissue_wait_timeout: "- {service_name}: --wait timed out after {wait_timeout}; bootroot-agent may still pick up the request on its next poll",
    warning_rotation_in_progress: "WARNING: rotation-state.json exists, indicating a CA key rotation is in progress.",
    error_trust_sync_blocked_by_rotation: "trust-sync is blocked while CA key rotation is in progress; complete or abort the rotation first",
    prompt_rotate_ca_key: "Rotate intermediate CA key? Current fingerprints:\n  root:         {root_fp}\n  intermediate: {inter_fp}\n[y/N]",
    prompt_rotate_ca_key_full: "Rotate root AND intermediate CA keys? Current fingerprints:\n  root:         {root_fp}\n  intermediate: {inter_fp}\nBoth will be replaced. [y/N]",
    rotate_ca_key_full_checklist: "Full CA key rotation checklist:\n  - Have you prepared to update root CA trust anchors distributed to external clients?\n  - Have you notified external systems that depend on this root CA?",
    error_rotation_state_corrupt: "rotation-state.json is corrupt or unreadable: {path}; manually inspect or delete the file to proceed",
    error_rotation_mode_mismatch: "rotation-state.json mode ({mode}) does not match the --full flag; complete or abort the existing rotation first",
    error_parse_cert_failed: "Failed to parse certificate {path}: {reason}",
    warning_stale_backup: "WARNING: Backup files (.bak) exist without rotation-state.json; a previous rotation may have been interrupted.",
    rotate_ca_key_resuming: "Resuming CA key rotation from phase {phase}.",
    rotate_ca_key_phase_backup: "[Phase 1] Backing up intermediate CA cert and key...",
    rotate_ca_key_phase_generate: "[Phase 2] Generating new intermediate CA key pair...",
    rotate_ca_key_phase_trust_additive: "[Phase 3] Distributing transitional trust (additive)...",
    rotate_ca_key_phase_restart_stepca: "[Phase 4] Restarting step-ca with new intermediate...",
    rotate_ca_key_registrar_internal_repair: "[Phase 4 tail] Replacing the bootroot-internal registrar credential under the new root...",
    rotate_registrar_internal_complete: "The bootroot-internal registrar credential has been repaired; its agent was signalled to reload.",
    rotate_registrar_internal_up_to_date: "The bootroot-internal registrar credential is complete and was issued under the active root; nothing to repair. Pass --force to replace it anyway.",
    prompt_rotate_registrar_internal: "Replace the bootroot-internal registrar credential (auth entry, leaf and stored root fingerprint)?",
    rotate_ca_key_phase_reissue: "[Phase 5] Re-issuing service certificates...",
    rotate_ca_key_phase_finalize: "[Phase 6] Finalizing trust (subtractive)...",
    rotate_ca_key_phase_cleanup: "[Phase 7] Cleaning up...",
    rotate_ca_key_skip_migrated: "- {service_name}: already issued by new intermediate, skipping",
    rotate_ca_key_reissue_remote_hint: "- {service_name}: remote-bootstrap service — run `bootroot-remote bootstrap` on the service host to reissue",
    rotate_ca_key_finalize_blocked: "Cannot finalize: the following services still have certificates issued by the old intermediate: {services}",
    warning_force_finalize: "WARNING: Forcing trust finalization with un-migrated services. These services may lose mTLS connectivity.",
    warning_force_finalize_full: "WARNING: Root fingerprint has changed and {services} remain un-migrated. mTLS will break immediately for these services.",
    rotate_ca_key_complete: "CA key rotation complete.\n  old intermediate: {old_fp}\n  new intermediate: {new_fp}",
    rotate_ca_key_complete_full: "CA key rotation complete (full).\n  old root:         {old_root_fp}\n  new root:         {new_root_fp}\n  old intermediate: {old_inter_fp}\n  new intermediate: {new_inter_fp}",
    rotate_ca_key_phase_generate_root: "[Phase 2a] Generating new root CA key pair...",
    rotate_ca_key_current_fingerprints: "Current CA fingerprints:\n  root:         {root_fp}\n  intermediate: {inter_fp}",
    rotate_ca_key_phase_skipped: "Phase {phase} already completed, skipping.",
    warning_rotate_ca_agent_restart_failed: "WARNING: could not restart OpenBao Agent container {container} ({error}); it converges on its own at the next static-secret render (within 30s). Rotation continues.",
    summary_responder_check_ok: "- responder check: ok",
    summary_responder_check_skipped: "- responder check: skipped",
    summary_db_check_ok: "- db check: ok",
    summary_db_check_skipped: "- db check: skipped",
    summary_db_host_resolution: "- db host resolution: {from} -> {to}",
    summary_kv_paths: "- OpenBao KV paths:",
    summary_approles: "- AppRoles:",
    summary_role_id: "    role_id: {value}",
    summary_secret_id: "    secret_id: {value}",
    summary_next_steps: "next steps:",
    next_steps_configure_templates: "  - Configure OpenBao Agent templates for step-ca, responder, and bootroot-agent.",
    next_steps_responder_template: "  - Responder template: {value}",
    next_steps_responder_config: "  - Responder config output: {value}",
    next_steps_responder_url: "  - Responder admin URL: {value}",
    next_steps_openbao_agent_stepca_config: "  - OpenBao Agent (step-ca): {value}",
    next_steps_openbao_agent_responder_config: "  - OpenBao Agent (responder): {value}",
    next_steps_openbao_agent_override: "  - OpenBao Agent compose override: {value}",
    next_steps_reload_services: "  - Start or reload step-ca and responder to consume rendered secrets.",
    next_steps_run_status: "  - Run `bootroot status` to verify services.",
    next_steps_eab_hint: "  - If your ACME CA requires EAB, provision kid/hmac externally (bundled OSS step-ca does not support EAB), then store them at {path} or rerun with --eab-kid/--eab-hmac.",
    infra_install_completed: "bootroot infra install: completed",
    infra_install_env_written: "bootroot infra install: .env written",
    infra_install_dirs_created: "bootroot infra install: directories created",
    error_infra_install_failed: "bootroot infra install failed",
    clean_completed: "bootroot clean: completed",
    clean_confirm: "This will remove containers, volumes, secrets, state.json, and .env. Continue? [y/N]: ",
    clean_confirm_certs: "Also remove certs/ directory? [y/N]: ",
    clean_confirm_openbao_only: "Remove the {container} container and its volume? [y/N]: ",
    clean_openbao_only_completed: "openbao container and volume removed; other services left running",
    error_init_partial_openbao_state: "OpenBao at {url} is already initialised but no root token is available.\n  Recovery options:\n    1. Re-run with --root-token <token> (or OPENBAO_ROOT_TOKEN=...) using the token saved from the prior init.\n    2. `bootroot clean --openbao-only` if this OpenBao instance is dedicated to bootroot and may be wiped.\n    3. Manual OpenBao operator action if the instance is shared.",
    error_clean_failed: "bootroot clean failed",
    error_env_parse_failed: "Failed to parse .env file: {value}",
    prompt_save_unseal_keys: "Save unseal keys to file for automatic unseal? [y/N]: ",
    openbao_unseal_keys_saved: "Unseal keys saved to {value}",
    openbao_unseal_keys_not_saved_warning: "WARNING: Unseal keys were NOT saved to a file. Copy and store them securely — they will not be shown again.",
    openbao_unseal_keys_deleted: "Unseal keys file deleted: {value}",
    error_openbao_save_unseal_keys_failed: "Failed to save unseal keys",
    error_openbao_delete_unseal_keys_failed: "Failed to delete unseal keys file",
    error_remove_dir_failed: "Failed to remove directory: {value}",
    infra_install_stepca_not_checked: "- step-ca: not checked (will be bootstrapped by init)",
    dns_alias_registered: "bootroot service add: registered HTTP-01 DNS alias {value}",
    dns_alias_replaying: "bootroot infra up: replaying {value} DNS alias(es) on {container}",
    dns_alias_responder_not_running: "WARNING: {container} container not running — skipping DNS alias registration. Run `bootroot infra up` first.",
    dns_alias_connect_failed: "Failed to reconnect {container} with DNS aliases",
    dns_alias_connect_rollback: "Reconnect failed; rolling back to restore {container} network connectivity",
    dns_alias_connect_recovered: "WARNING: DNS alias registration failed but network connectivity was restored. Run `bootroot infra up` to apply aliases later. ({error})",
    dns_alias_rollback_failed: "CRITICAL: rollback also failed — {container} is detached from network {network}. Reconnect manually: docker network connect {network} {container} ({error})",
    dns_alias_network_not_found: "No Docker network found for container {value}",
    dns_alias_responder_ambiguous: "{count} HTTP-01 responder containers match compose project `{project}`; refusing to pick one arbitrarily. Remove the stale container, or reinstall the other instance with a distinct `--instance-name`.",
    error_instance_name_invalid: "Invalid --instance-name `{name}`: use lowercase ASCII letters, digits and `-`, starting with a letter or digit, at most {max} characters",
    error_service_update_failed: "bootroot service update failed",
    error_service_update_no_flags: "No update flags specified; pass --secret-id-ttl, --secret-id-wrap-ttl, --no-wrap, --rn-cidrs, --cert-group, --reload-style, or --post-renew-command",
    service_update_summary: "bootroot service update: summary",
    service_update_field_changed: "- {field}: {old} -> {new}",
    service_update_rotate_hint: "Run `bootroot rotate approle-secret-id` for the new policy to take effect.",
    hint_secret_id_ttl_rotation_cadence: "NOTE: Ensure the secret_id TTL is at least 2× your rotation interval. For example, with a 12h rotation schedule, use a TTL of 24h or more. This buffer accounts for missed runs, maintenance windows, and restart recovery.",
    service_info_secret_id_ttl: "- secret_id TTL: {value}",
    service_info_secret_id_wrap_ttl: "- secret_id wrap TTL: {value}",
    service_info_token_bound_cidrs: "- token_bound_cidrs: {value}",
    policy_label_inherit: "inherit",
    policy_label_disabled: "disabled",
    policy_label_default_wrap_ttl: "{value} (default)",
    service_update_no_changes: "No fields changed.",
    service_update_hook_value_none: "(none)",
    service_remove_plan_header: "bootroot service remove: teardown plan for {value}",
    service_remove_plan_approle: "- AppRole to delete: {value}",
    service_remove_plan_policy: "- policy to delete: {value}",
    service_remove_plan_kv: "- KV path to delete: {value}",
    service_remove_plan_artifact: "- on-disk artifact to delete: {value}",
    service_remove_plan_agent_config: "- managed profile block to strip from: {value}",
    service_remove_plan_artifacts_preserved: "- on-disk cert/key/agent config preserved (pass --delete-artifacts to also remove them)",
    service_remove_dry_run: "Dry run: no changes made to state.json or OpenBao.",
    service_remove_confirm_prompt: "Remove service {value}? This deregisters its AppRole, policy, and KV material",
    service_remove_requires_yes: "Refusing to remove {value} without confirmation on a non-interactive stdin; pass --yes (alias --force) to proceed",
    service_remove_aborted: "Aborted; {value} was not removed.",
    service_remove_resource_removed: "removed: {value}",
    service_remove_resource_absent: "already absent: {value}",
    service_remove_resource_failed: "FAILED to remove {value}: {error}",
    service_remove_success: "Service {value} removed. Re-add with `bootroot service add ...` to change its delivery-mode.",
    service_remove_partial_failure: "Teardown incomplete for {value}; the state.json entry was kept so a re-run of `bootroot service remove --registration-id {value}` can finish the remaining deletions.",
    hint_consumer_reload_required: "Consumer reload/restart required (post-renew hook status):",
    hint_consumer_reload_service_with_hook: "- {registration_id}: post-renew hook configured ({hook})",
    hint_consumer_reload_service_without_hook: "- {registration_id}: NO post-renew hook configured; consumer may keep serving the previous cert via an open file descriptor (issue #614).",
    hint_consumer_reload_remediation: "Install a hook in place without re-onboarding: `bootroot service update --registration-id <id> --reload-style {sighup|systemd|docker-restart} --reload-target <process|unit|container>` (or use the low-level `--post-renew-command` form).",
    hint_reinit_reload_style: "When re-running `bootroot service add ...`, pass `--reload-style sighup|systemd|docker-restart --reload-target <target>` so a post-renew hook is registered up front — otherwise consumers can keep serving the previous cert through an open file descriptor after later rotations (issue #614).",
    error_openbao_bind_wildcard_required: "Binding OpenBao to a wildcard address (0.0.0.0 or [::]) requires --openbao-bind-wildcard to confirm wildcard intent",
    error_openbao_bind_tls_flag_required: "Non-loopback OpenBao binding requires --openbao-tls-required to acknowledge mandatory TLS",
    error_openbao_bind_tls_missing: "Non-loopback OpenBao binding requires TLS: {details}",
    error_openbao_bind_invalid_format: "Invalid --openbao-bind value: expected <IP>:<port> (e.g. 192.168.1.10:8200)",
    error_openbao_bind_ipv6_requires_brackets: "IPv6 addresses in --openbao-bind must be enclosed in brackets (e.g. [::1]:8200, [2001:db8::10]:8200)",
    error_openbao_advertise_addr_required: "Wildcard OpenBao bind (0.0.0.0 or [::]) requires --openbao-advertise-addr <IP:port> so remote bootstrap artifacts contain a reachable address",
    error_openbao_advertise_addr_invalid: "Invalid --openbao-advertise-addr value: expected <IP>:<port> (e.g. 192.168.1.10:8200)",
    error_openbao_advertise_addr_ipv6_requires_brackets: "IPv6 addresses in --openbao-advertise-addr must be enclosed in brackets (e.g. [fd12::1]:8200, [2001:db8::10]:8200)",
    error_openbao_advertise_addr_not_reachable: "--openbao-advertise-addr must be a specific routable address (not wildcard or loopback)",
    error_openbao_advertise_addr_specific_bind_rejected: "--openbao-advertise-addr is only valid with wildcard binds (0.0.0.0 or [::]); for specific-IP binds the bind address is used directly",
    error_openbao_override_file_missing: "Non-loopback OpenBao bind intent is recorded in state but the compose override file is missing; re-run infra install --openbao-bind to regenerate it",
    error_openbao_override_binding_mismatch: "OpenBao compose override port mapping does not match stored bind intent: expected {expected}, found {actual}; re-run infra install --openbao-bind to regenerate",
    info_openbao_bind_intent_recorded: "OpenBao non-loopback bind intent recorded: {addr}",
    info_openbao_bind_intent_cleared: "Previous OpenBao non-loopback bind intent cleared (re-install without --openbao-bind)",
    error_http01_admin_bind_requires_responder: "--http01-admin-bind requires the bootroot-http01 service in the compose file",
    error_http01_admin_bind_invalid_format: "--http01-admin-bind must be a valid <IP>:<port> address",
    error_http01_admin_bind_wildcard_required: "--http01-admin-bind-wildcard is required when binding to 0.0.0.0 or [::]",
    error_http01_admin_bind_tls_flag_required: "--http01-admin-tls-required is required when --http01-admin-bind specifies a non-loopback address",
    error_http01_admin_bind_ipv6_requires_brackets: "--http01-admin-bind IPv6 addresses must be bracketed, e.g. [::1]:8080",
    error_http01_admin_advertise_addr_required: "Wildcard HTTP-01 admin bind (0.0.0.0 or [::]) requires --http01-admin-advertise-addr <IP:port> so the TLS certificate includes a routable SAN",
    error_http01_admin_advertise_addr_invalid: "Invalid --http01-admin-advertise-addr value: expected <IP>:<port> (e.g. 192.168.1.10:8080)",
    error_http01_admin_advertise_addr_ipv6_requires_brackets: "IPv6 addresses in --http01-admin-advertise-addr must be enclosed in brackets (e.g. [fd12::1]:8080, [2001:db8::10]:8080)",
    error_http01_admin_advertise_addr_not_reachable: "--http01-admin-advertise-addr must be a specific routable address (not wildcard or loopback)",
    error_http01_admin_advertise_addr_specific_bind_rejected: "--http01-admin-advertise-addr is only valid with wildcard binds (0.0.0.0 or [::]); for specific-IP binds the bind address is used directly",
    info_http01_admin_bind_intent_recorded: "HTTP-01 admin non-loopback bind intent recorded: {addr}",
    info_http01_admin_bind_intent_cleared: "Previous HTTP-01 admin non-loopback bind intent cleared (re-install without --http01-admin-bind)",
    error_http01_admin_override_file_missing: "Non-loopback HTTP-01 admin bind intent is recorded in state but the compose override file is missing; re-run infra install --http01-admin-bind to regenerate it",
    error_http01_admin_override_binding_mismatch: "HTTP-01 admin compose override port mapping does not match stored bind intent: expected {expected}, found {actual}; re-run infra install --http01-admin-bind to regenerate",
    error_http01_admin_bind_tls_missing: "HTTP-01 admin API requires TLS for non-loopback binding but TLS prerequisites are not met: {details}",
    error_stepca_bind_requires_stepca: "--stepca-bind requires the step-ca service in the compose file",
    error_stepca_bind_invalid_format: "--stepca-bind must be a valid <IP>:<port> address",
    error_stepca_bind_wildcard_required: "--stepca-bind-wildcard is required when binding to 0.0.0.0 or [::]",
    error_stepca_bind_ipv6_requires_brackets: "--stepca-bind IPv6 addresses must be bracketed, e.g. [::1]:9000",
    error_stepca_advertise_addr_required: "Wildcard step-ca bind (0.0.0.0 or [::]) requires --stepca-advertise-addr <IP:port> so remote nodes have a routable ACME directory address",
    error_stepca_advertise_addr_invalid: "Invalid --stepca-advertise-addr value: expected <IP>:<port> (e.g. 192.168.1.10:9000)",
    error_stepca_advertise_addr_ipv6_requires_brackets: "IPv6 addresses in --stepca-advertise-addr must be enclosed in brackets (e.g. [fd12::1]:9000, [2001:db8::10]:9000)",
    error_stepca_advertise_addr_not_reachable: "--stepca-advertise-addr must be a specific routable address (not wildcard or loopback)",
    error_stepca_advertise_addr_specific_bind_rejected: "--stepca-advertise-addr is only valid with wildcard binds (0.0.0.0 or [::]); for specific-IP binds the bind address is used directly",
    info_stepca_bind_intent_recorded: "step-ca non-loopback bind intent recorded: {addr}",
    info_stepca_bind_intent_cleared: "Previous step-ca non-loopback bind intent cleared (re-install without --stepca-bind)",
    error_stepca_override_file_missing: "Non-loopback step-ca bind intent is recorded in state but the compose override file is missing; re-run infra install --stepca-bind to regenerate it",
    error_stepca_override_binding_mismatch: "step-ca compose override port mapping does not match stored bind intent: expected {expected}, found {actual}; re-run infra install --stepca-bind to regenerate",
    info_openbao_tls_provisioned: "OpenBao TLS server certificate issued: {path}",
    info_openbao_hcl_tls_written: "OpenBao HCL updated with TLS configuration",
    info_openbao_hcl_tls_reverted: "OpenBao HCL restored to plaintext (TLS disabled)",
    error_openbao_tls_provision_failed: "Failed to issue OpenBao TLS server certificate",
    error_openbao_tls_output_dir_symlink: "Refusing to use a symlink as the OpenBao TLS output directory: {path}",
    error_openbao_hcl_write_failed: "Failed to write OpenBao HCL configuration",
    info_http01_admin_tls_reverted: "Responder config restored to plaintext (TLS disabled until next init)",
    info_http01_admin_tls_provisioned: "HTTP-01 admin API TLS server certificate issued: {path}",
    error_http01_admin_tls_provision_failed: "Failed to issue HTTP-01 admin API TLS server certificate",
    info_infra_tls_renewed: "Infrastructure certificate renewed: {name}",
    info_infra_tls_reload: "Reloading service after certificate renewal: {strategy}",
    info_infra_tls_verified: "Certificate reload verified for {name}: the OpenBao listener now serves the renewed certificate",
    error_infra_tls_renew_failed: "Failed to renew infrastructure certificate: {name}",
    error_infra_tls_openbao_url_not_https: "An openbao infra-cert entry is registered but state openbao_url is not a usable https URL: {url}; state is internally inconsistent",
    error_infra_tls_probe_unreachable: "Certificate reload verification for {name} could not reach the OpenBao listener at {addr} within the deadline",
    error_infra_tls_probe_mismatch: "Certificate reload verification for {name} failed: the OpenBao listener still serves the previous certificate after the deadline",
    prompt_rotate_infra_tls: "Renew infrastructure certificates? [y/N]: ",
    rotate_infra_tls_no_entries: "No infrastructure certificates registered in state",
    reinit_plan_title: "bootroot reinit: recovery plan",
    reinit_plan_destructive_actions: "Destructive actions:",
    reinit_plan_destructive_container: "  - stop and remove the {container} container",
    reinit_plan_destructive_volumes: "  - remove openbao-data and openbao-audit volumes (OpenBao state will be wiped)",
    reinit_plan_destructive_state_file: "  - rewrite state.json with intent-only fields (services / approles / policies cleared)",
    reinit_plan_destructive_runtime_files: "  - remove {secrets_dir}/openbao/unseal-keys.txt and per-service openbao-agent runtime files",
    reinit_plan_destructive_service_creds: "  - remove stale per-service AppRole credential files (role_id, secret_id) under {secrets_dir}/services/",
    reinit_plan_preserved_actions: "Preserved:",
    reinit_plan_preserved_ca: "  - {secrets_dir}/config/ca.json and step-ca root/intermediate CA key material",
    reinit_plan_preserved_password: "  - {secrets_dir}/password.txt (step-ca password is NOT regenerated)",
    reinit_plan_preserved_postgres: "  - PostgreSQL container, volume, and step-ca database state",
    reinit_plan_preserved_compose_overrides: "  - operator-authored OpenBao compose overrides under {secrets_dir}/openbao/",
    reinit_plan_preserved_intent: "  - non-loopback OpenBao / HTTP-01 admin / step-ca bind intent recorded in state.json",
    reinit_plan_preserved_intent_section: "Preserved deployment intent (snapshotted from state.json):",
    reinit_plan_preserved_intent_secrets_dir: "  - secrets_dir: {value}",
    reinit_plan_preserved_intent_openbao_bind: "  - openbao_bind_addr: {value}",
    reinit_plan_preserved_intent_openbao_advertise: "  - openbao_advertise_addr: {value}",
    reinit_plan_preserved_intent_http01_bind: "  - http01_admin_bind_addr: {value}",
    reinit_plan_preserved_intent_http01_advertise: "  - http01_admin_advertise_addr: {value}",
    reinit_plan_preserved_intent_stepca_bind: "  - stepca_bind_addr: {value}",
    reinit_plan_preserved_intent_stepca_advertise: "  - stepca_advertise_addr: {value}",
    reinit_plan_preserved_intent_infra_certs: "  - infra_certs: {count} entry(ies) preserved",
    reinit_plan_preserved_intent_none: "  - (no prior state.json snapshot — running on a clean/rsync-cloned tree)",
    reinit_plan_service_registry_warning: "After reinit completes, the service registry will be empty; re-run `bootroot service add` for each service that was previously registered.",
    reinit_confirm: "Proceed with reinit? [y/N]: ",
    reinit_completed: "bootroot reinit: completed",
    reinit_service_registry_post_summary: "Service registry is empty after reinit. Re-run `bootroot service add ...` for each affected service.",
    error_reinit_failed: "bootroot reinit failed",
    error_reinit_external_openbao: "Refusing to reinit: compose file {path} does not declare an `openbao` service. `bootroot reinit` only operates on compose-managed local OpenBao. External or shared OpenBao must use an operator-managed runbook.",
    error_reinit_container_project_mismatch: "Refusing to reinit: container `{container}` belongs to compose project `{actual}`, but the current work directory derives project `{expected}`. Refusing to wipe an OpenBao that is not managed by this work directory.",
    error_reinit_container_missing_compose_label: "Refusing to reinit: container `{container}` exists but does not carry the `{label}` compose label. `bootroot reinit` only operates on compose-managed local OpenBao; a container without compose labels cannot be proven to belong to this work directory's compose project. If this container is stale, remove it manually before retrying.",
    error_reinit_root_token_output_unsafe: "Refusing to write root token: existing file {path} has world-/group-readable permissions. Run `chmod 0600 {path}` or remove the file before retrying.",
    error_reinit_root_token_output_not_file: "Refusing to write root token: {path} exists and is not a regular file. Pass a regular-file destination for `--root-token-output`.",
    error_reinit_root_token_output_unwritable: "Refusing to start reinit: cannot write to `--root-token-output` destination {path} ({reason}). Pass a writable file path before retrying.",
    error_reinit_root_token_persist_failed: "WARNING: failed to persist the freshly issued OpenBao root token to {path} ({reason}). OpenBao has been initialised; the token below is shown unmasked so it is not lost — capture it now and store it securely.\nROOT_TOKEN={token}",
    error_reinit_summary_json_not_file: "Refusing to start reinit: --summary-json destination {path} exists and is not a regular file. Pass a regular-file path before retrying.",
    error_reinit_summary_json_unwritable: "Refusing to start reinit: cannot write to `--summary-json` destination {path} ({reason}). The summary JSON carries the freshly issued root token and unseal keys; an unwritable destination would recreate the partial-init trap. Pass a writable file path before retrying.",
    error_reinit_summary_json_unsafe: "Refusing to start reinit: existing `--summary-json` destination {path} has world-/group-readable permissions. The summary JSON carries the freshly issued root token and unseal keys; writing into a wider-than-0600 file would briefly expose those secrets. Run `chmod 0600 {path}` or remove the file before retrying.",
    error_reinit_explicit_openbao_url: "Refusing to start reinit: explicit `--openbao-url {url}` is not accepted because `bootroot reinit` only operates on compose-managed local OpenBao. The reinit target URL is derived from the compose-managed service (and the snapshotted non-loopback bind, if any). Re-run without `--openbao-url`; if recovery against an external/shared OpenBao is needed, follow the operator runbook instead.",
    error_reinit_stepca_password_missing_with_ca_material: "Refusing to start reinit: `{password_path}` is missing but at least one file `step ca init` writes is still preserved under `{secrets_dir}`: {preserved_paths}. Preserved CA *keys* (`secrets/root_ca_key`, `secrets/intermediate_ca_key`) are encrypted with the original password, so generating a fresh one under `reinit_mode` would produce a deployment whose `password.txt` cannot unlock them — any later `step certificate create --ca-password-file /home/step/password.txt` would fail. Any other preserved file `step ca init` writes (`config/ca.json`, `config/defaults.json`, `certs/root_ca.crt`, `certs/intermediate_ca.crt`) is equally blocking even without encrypted key material, because the second init pass's `step ca init` cannot complete cleanly when one of its targets already exists (it generates fresh cert/key files and then exits non-zero on TTY-bound overwrite confirmation), recreating the partial-init trap after `OpenBao` has already been wiped. Restore `password.txt` from a backup before retrying; if the original password is unrecoverable, remove every preserved step-ca artifact listed above so reinit can generate a fresh CA from scratch.",
    error_audit_store_agent_config_required: "Refusing to proceed: `{command}` needs `--agent-config <path>` on this host, because the registrar endpoint is recorded as enabled in state.json or a rendered OpenBao audit compose override is present. `[registrar] audit_store_dir` in the bootroot-agent configuration file is the only definition of where the audit store lives, and `[registrar_endpoint] enabled` in that same file is cross-checked against state.json. Nothing was created, rendered or deleted. Re-run with `--agent-config` naming the configuration the daemon runs with.",
    error_audit_store_agent_config_unreadable: "Refusing to proceed: the `--agent-config` file {path} could not be read ({reason}). Nothing was created, rendered or deleted.",
    error_audit_store_agent_config_malformed: "Refusing to proceed: the `--agent-config` file {path} is not valid TOML ({reason}). Nothing was created, rendered or deleted.",
    error_audit_store_agent_config_undeserializable: "Refusing to proceed: the `--agent-config` file {path} has a `[registrar]` or `[registrar_endpoint]` table bootroot-agent itself would refuse ({reason}). Both tables reject unknown keys, so a misspelling such as `audit_store_dirr` or `enable` fails here rather than being read as a default. Nothing was created, rendered or deleted.",
    error_audit_store_agent_config_rejected: "Refusing to proceed: the `[registrar]` table in the `--agent-config` file {path} is invalid ({reason}). Nothing was created, rendered or deleted.",
    error_audit_store_enablement_mismatch: "Refusing to proceed: the registrar endpoint's enablement disagrees between the two places it is recorded. {state_path} records `registrar_endpoint.enabled = {state_value}`, while {config_path} sets `[registrar_endpoint] enabled = {config_value}`. bootroot does not pick a winner and rewrites neither source: binding OpenBao's mandatory audit device into a store for a daemon whose own configuration says the endpoint does not exist would put the audit path somewhere that configuration does not account for. Nothing was created, rendered or deleted. Make the two agree and re-run.",
    error_audit_store_not_privileged: "Refusing to proceed: provisioning the audit store at {path} requires running as uid {expected_uid}, but this `bootroot init` is running as uid {found_uid}. The store directory and its `records/` subdirectory are created owned by the process that creates them, so an unprivileged run would leave behind a store that bootroot itself refuses as foreign-owned on the next attempt — and it repairs neither an owner nor a mode. Nothing was created, rendered or deleted. Re-run as root, for example with `sudo bootroot init --agent-config <path>`.",
    error_audit_store_override_stale: "Refusing to proceed: the rendered OpenBao audit compose override binds the audit store at {rendered}, but the `--agent-config` file now resolves `[registrar] audit_store_dir` to {configured}. bootroot does not silently re-point the mount, because moving the store leaves the old audit history behind. Nothing was created, rendered or deleted. To move a provisioned store: stop the stack, relocate the directory, delete the rendered override, then re-run `bootroot init`.",
    error_audit_store_override_unreadable: "The rendered OpenBao audit compose override {path} declares no bind mount at /openbao/audit, so the audit store it names cannot be read back. Delete the file and re-run `bootroot init` as root to render it again.",
    error_audit_store_override_missing: "The registrar endpoint is recorded as enabled in state.json but the rendered OpenBao audit compose override is missing; run `bootroot init` as root with `--agent-config` to provision the audit store and render it.",
    error_audit_store_dir_invalid: "Refusing to proceed: the audit store directory {path} is {found}. It must be a directory, not a symbolic link, owned by uid {expected_uid} and at mode exactly {expected_mode}. bootroot repairs neither an owner nor a mode here — widening a store directory silently is how a store stops being one. Fix it with `chown 0:0 {path}` followed by `chmod 0700 {path}`, then re-run.",
    error_audit_store_dir_unusable: "The audit store directory {path} is {found}. It must be a directory, not a symbolic link, owned by uid {expected_uid} and at mode exactly {expected_mode}. `bootroot infra up` creates nothing and repairs nothing here. Fix it with `chown 0:0 {path}` followed by `chmod 0700 {path}`, or run `bootroot init` as root with `--agent-config` to provision the store, then re-run `bootroot infra up`.",
    error_audit_store_ancestor_invalid: "Refusing to proceed: {path}, a directory above the audit store, is {found}. Every existing component above the store must be a directory, not a symbolic link, carrying the world-execute bit, so an unprivileged `bootroot infra up` can reach the store directory to check it. Nothing was created and no override was rendered.",
    error_audit_store_openbao_invalid: "Refusing to proceed: {path} is {found}. That path is about to become the bind-mount source for OpenBao's file audit device, so it must be a directory and not a symbolic link. Its owner and mode are not bootroot's to assert — the OpenBao container's entrypoint sets them on the first start. Nothing was created and no override was rendered.",
    error_audit_store_layout_failed: "Refusing to proceed: the audit store layout at {path} could not be prepared ({reason}).",
    error_audit_store_dir_unrenderable: "Refusing to proceed: `[registrar] audit_store_dir` ({path}) cannot be rendered as a supported Docker Compose bind mount. A control character has no spelling Compose reads back. A colon has its own `source:` field in the current long bind form, but bootroot still refuses it because the reader (including existing short-form overrides), systemd escaping, and rendered shell commands must all carry the store path; widening that input contract is outside this change. Choose a store directory whose path is printable and carries no colon.",
    audit_store_fault_symlink: "a symbolic link",
    audit_store_fault_not_directory: "not a directory",
    audit_store_fault_not_traversable: "a directory at mode {mode} and so not world-traversable",
    audit_store_fault_owner: "owned by uid {found} rather than uid {expected}",
    audit_store_fault_mode: "at mode {found} rather than mode {expected}",
    error_audit_reserve_store_path_unrenderable: "Refusing to proceed: `[registrar] audit_store_dir` ({path}) cannot be carried by the audit store's systemd artifacts, because it {fault}. The generated `.mount` unit spells the store path three times — as the unit's own name, as `Where=`, and as the mount point the rendered commands operate on — and a path systemd would read back differently from the one configured leaves the mount, the unit name and the configuration describing different directories. Nothing was created and nothing was rendered. Choose a store directory whose path carries none of those, or set `audit_store_enforcement = \"directory\"`.",
    audit_reserve_path_fault_line_break: "holds a newline or a carriage return, which a line-based unit file cannot hold",
    audit_reserve_path_fault_edge_whitespace: "begins or ends with whitespace, which systemd strips off the value",
    audit_reserve_path_fault_final_backslash: "ends with a backslash, which the unit-file reader takes as a line continuation",
    error_audit_reserve_image_parentless: "Refusing to proceed: `[registrar] audit_store_dir` ({path}) has no parent directory, so the loopback image — a sibling of the store, never inside the filesystem it backs — has nowhere to live. Nothing was created and nothing was rendered.",
    error_audit_reserve_staging_inside_store: "Refusing to proceed: the audit store's three systemd artifacts would be staged at {staging}, which is inside `[registrar] audit_store_dir` ({store}). The mount hides everything beneath that directory, so the artifacts the operator needs would disappear the moment the reserve came up — and on the next run the store would no longer be empty, which is refused rather than mounted over. Nothing was created and nothing was rendered. Move the deployment's Compose file out of the audit store, or choose an `audit_store_dir` that does not contain it.",
    error_audit_reserve_below_minimum: "Refusing to proceed: `[registrar] audit_store_reserve_bytes` is {configured} bytes, which does not clear the {minimum} bytes the audit store at {path} needs in `filesystem` mode. The minimum is the larger of a fixed {floor}-byte floor, which keeps `mkfs.ext4` clear of the sizes at which it refuses outright or quietly omits the journal, and {records} bytes, the worst case the daemon's own records reach at `audit_max_file_bytes` × (`audit_max_retained_files` + 1) — compared strictly, because usable space inside an image never exceeds the image itself. Clearing the minimum is NOT a claim that the reserve is big enough: an ext4 filesystem offers less than the image it sits in, and OpenBao's audit device has no bound of its own, so sizing the reserve so both writers fit stays yours. Nothing was created and nothing was rendered. Raise `audit_store_reserve_bytes` above the minimum, or set `audit_store_enforcement = \"directory\"`.",
    error_audit_reserve_minimum_unreachable: "Refusing to proceed: no `audit_store_reserve_bytes` this build accepts can hold the audit store at {path}. `audit_max_file_bytes` ({max_file_bytes}) × (`audit_max_retained_files` ({max_retained_files}) + 1) is at or above the `i64::MAX` ceiling the reserve itself is capped at, so the daemon's rotation alone could fill any image that could be created. Raising the reserve is not a remedy that can be followed. Nothing was created and nothing was rendered. Lower `audit_max_file_bytes` or `audit_max_retained_files`, or set `audit_store_enforcement = \"directory\"`.",
    error_audit_reserve_image_not_regular: "Refusing to proceed: the audit store's loopback image path {path} is {found}. bootroot overwrites nothing there: the derived path is a sibling of the store and what occupies it was put there by somebody. Nothing was created and nothing was rendered. Move or remove what is at that path, or choose another `audit_store_dir`.",
    error_audit_reserve_image_size_mismatch: "Refusing to proceed: the audit store's loopback image {path} is {found} bytes, and `[registrar] audit_store_reserve_bytes` is {expected}. An existing image is never resized, truncated or reformatted in place, in either direction — reformatting destroys the records the reserve exists to keep, and a silent grow would leave the mount unit, the image and the configuration describing three different ceilings. A changed reserve is answered by replacing the image, in a maintenance window with both writers stopped. Nothing was created and nothing was rendered. See \"The shared audit store\" in the operations manual.",
    error_audit_reserve_free_space: "Refusing to proceed: allocating the audit store's loopback image {path} needs {outstanding} more bytes, and the filesystem it would be created on has {available} available. The figure compared is what is still outstanding, not the configured reserve of {reserve}: a host that has already spent the reserve on the image has free space below it as its normal steady state. Nothing was created and nothing was rendered.",
    error_audit_reserve_underlying_too_large: "Refusing to proceed: the audit store directory {path} already holds {used} bytes on the underlying filesystem, more than the {reserve}-byte reserve. No image, no mount and no relocation can make that store fit inside the reserve. Nothing was created and nothing was rendered. Raise `audit_store_reserve_bytes`, reduce what the store holds, or set `audit_store_enforcement = \"directory\"`.",
    error_audit_reserve_arithmetic: "Refusing to proceed: {figure} cannot be represented as a 64-bit byte count, so the audit store's preflight cannot be decided. A byte figure that does not fit fails the run rather than wrapping, because a wrapped product is a preflight that passes a host with no room. Nothing was created and nothing was rendered.",
    error_audit_reserve_unreadable: "Refusing to proceed: {path} could not be read while provisioning the audit store's reserve ({reason}). This is reported as unreadable and never as absent — \"cannot see it\" is not \"it is not there\", which would read as an unprovisioned host. Nothing was created and nothing was rendered.",
    audit_reserve_figure_allocated: "the allocated size of {path}",
    audit_reserve_figure_underlying: "the size of what {path} holds",
    audit_reserve_figure_available: "the space available on the filesystem holding {path}",
    audit_reserve_kind_regular: "a regular file",
    audit_reserve_kind_directory: "a directory",
    audit_reserve_kind_symlink: "a symbolic link",
    audit_reserve_kind_other: "neither a regular file nor a directory",
    audit_reserve_mount_description: "a {fs_type} filesystem from {source}",
    audit_reserve_outcome_directory: "Audit store reserve: unenforced (directory). `[registrar] audit_store_enforcement` is explicitly configured as `directory`, so the store at {store} is a plain directory and the {reserve}-byte `audit_store_reserve_bytes` is a budget with no kernel-enforced ceiling behind it. Nothing here creates an image, a mount unit or an ordering drop-in, and nothing looks for one a previous `filesystem`-mode run may have left. An XFS or ext4 project quota applied to the store directory is the documented route to a real ceiling in this mode, and composes with everything above unchanged.",
    audit_reserve_outcome_enforced: "Audit store reserve: enforced (filesystem). {store} is mounted from the loopback image {image} of {size} bytes through the unit {unit}, which is enabled and restored on boot. Writes past the reserve now fail with ENOSPC on the reserve rather than filling the host's root filesystem.",
    audit_reserve_outcome_not_activated: "Audit store reserve: provisioned, not activated (filesystem). The audit store at {store} does not yet have the reserve behind it, so this run stops here rather than reporting success. The mode is not rewritten and the run does not continue as `directory`.",
    audit_reserve_openbao_owner_caveat: "Note: `openbao/`'s ownership is not verified. Its owner is the OpenBao container entrypoint's to set on the first container start, and nothing supplies that uid to a phase running before any Docker call. Check it by hand once the stack is up.",
    audit_reserve_outstanding_header: "Outstanding:",
    audit_reserve_artifacts_header: "Rendered artifacts (bootroot wrote these; it installed none of them):",
    audit_reserve_steps_header: "Run these as root, in order, then run the command in the last step:",
    audit_reserve_no_steps: "No commands are rendered for this state, and that is a refusal rather than an absence: a path one of the renames would target already exists, and bootroot renames onto nothing. `mv dir dir.pre-mount` where the destination is an existing directory does not replace it — it moves the source inside it — so an unchecked sequence would nest one store in another and the next copy would read the wrong tree. Deal with the path named above by hand, then run this command again.",
    audit_reserve_no_steps_unremediable: "No command is rendered for this state either, and nothing is being withheld: what is outstanding above is at a path bootroot neither removes nor replaces, because what is there may be the only copy of something. It is named and left exactly as it was. Resolve it by hand, then run `{command}` again.",
    audit_reserve_note_stacked: "Note: more than one mount entry names {store}. The active mount is the last of them; the ones beneath it are hidden and are not what any reader reaches.",
    audit_reserve_finding_store_not_empty: "{store} already holds content on the underlying filesystem. On a live `bootroot infra up`, bring-up is refused before the stack starts, so no container starts. `create_host_path: false` governs creation only: if the underlying {store}/openbao already exists and the mount fails at boot, Compose still binds it and the audit device writes on the root filesystem. This exposure remains until the records are relocated onto the reserve, and completing the relocation is what retires it. Mounting the reserve over the store would hide every byte of it from every reader, this verification included, and what is hidden may be the only copy of records written before the reserve existed. Nothing was deleted, moved or mounted over. There are two supported ways forward: relocate those records onto the reserve, which is what the steps below open — stop both writers for a scheduled maintenance window, rename the store aside into {holding}, and re-run this command, which then renders the activation and the copy — or set `audit_store_enforcement = \"directory\"` and run without a kernel-enforced ceiling. Restarting OpenBao afterwards costs a manual shamir unseal.",
    audit_reserve_finding_image_absent: "The loopback image {path} does not exist.",
    audit_reserve_finding_image_sparse: "The loopback image {path} is sparse: {missing} of its {size} bytes are not allocated. A sparse image passes every size check and still lets the writers consume the root filesystem lazily as records arrive.",
    audit_reserve_finding_image_owner: "The loopback image {path} is owned by uid {found} rather than uid {expected}. A principal that can rewrite the image can rewrite the audit filesystem underneath both writers.",
    audit_reserve_finding_image_mode: "The loopback image {path} is at mode {found} rather than mode {expected}.",
    audit_reserve_finding_artifact_absent: "{path} is not installed.",
    audit_reserve_finding_artifact_differs: "{path} is installed and differs from what this run rendered. Presence proves nothing on its own: an edited unit can name another image or mount point, and an edited drop-in can have lost its `After=`. The running mount is not restarted over a text difference — the unit's text governs the next boot.",
    audit_reserve_finding_mount_absent: "Nothing is mounted at {store}.",
    audit_reserve_finding_mount_foreign: "{store} is mounted, but not from the reserve: what is there is {found}. A tmpfs, a bind mount or a loop device backed by another file is not the reserve.",
    audit_reserve_finding_subdir_absent: "{path} does not exist on the mounted filesystem.",
    audit_reserve_finding_subdir_invalid: "{path} on the mounted filesystem is {found}.",
    audit_reserve_step_stop_writers: "Stop both writers: the Compose stack, so no container holds a bind under the store, and `bootroot-registrar.service`, so no verb record is written while the mount is being established.",
    audit_reserve_step_image: "Create or repair the loopback image. If an already mounted reserve is sparse and `fallocate` is unavailable or fails, the rendered fallback unmounts it before the in-place repair; the following activation step mounts it again.",
    audit_reserve_step_install: "Install the rendered artifacts and activate the mount.",
    audit_reserve_step_subdirectories: "Create the store's subdirectories on the now-mounted filesystem. No `chown` is rendered for `openbao/`: its owner is the OpenBao entrypoint's to set on the first container start.",
    audit_reserve_step_rerun: "Run `{command}` again. It verifies the result, reports enforced and continues where this run stopped.",
    audit_reserve_outcome_migration_incomplete: "Audit store reserve: migration incomplete (filesystem). A holding directory beside {store} says the store\'s contents are part-way onto the reserve, so this run fails here rather than reporting any other outcome: however complete the image, the artifacts and the mount are, the reserve is not enforced while a migration is open, and no container is started. Nothing was deleted, moved or mounted over — bootroot never deletes, moves or modifies a holding directory, and every step below is yours to run.",
    audit_reserve_directory_migration_open: "Note: {holding} exists. `audit_store_enforcement` is `directory`, so none of the reserve\'s enforcement applies and this raises no outcome — but that directory is an unfinished relocation of {store}, and what it holds may be the only copy of those records. It is not debris. Finish the relocation under `filesystem` mode, or move its contents somewhere you have decided on, before removing it.",
    audit_reserve_finding_migration_holding: "{holding} exists, so a migration of {store} onto the reserve is in progress. Its presence is checked before every other verdict, because an operator who completes the aside rename and stops there leaves a valid image, a valid mount and an empty store — which would otherwise read as enforced over an audit trail stranded in a sibling directory. bootroot neither deletes, moves nor modifies it: the only two changes sanctioned here are the closing rename, once the type guard and all three comparisons have exited zero, and the rollback rename, while the migration is still open.",
    audit_reserve_finding_migration_store_not_empty: "{store} still holds content while {holding} already exists, so no activation, no copy, no closing rename and no rollback is rendered. Either the aside rename never ran and {holding} was already there — a pre-existing holding path is a refusal, never an overwrite — or both writers were left up and wrote into the store again. Nothing bootroot could render is safe here: mounting the reserve would hide those records, the copy would move an unrelated tree onto it, and the rollback would move {holding} inside {store} rather than replacing it, because that is what a rename onto an existing directory does. Stop both writers, then move whichever of the two paths is not the store's records out of the way by hand, and run this command again. bootroot will not touch either.",
    audit_reserve_finding_migration_path_exists: "{path} already exists, so the rename that would target it is not rendered. A rename onto an existing directory does not replace it — it moves the source inside it — and a nested store is a tree the next copy reads wrongly. Move or deal with that path by hand first; bootroot will not touch it.",
    audit_reserve_finding_migration_mount_absent: "The reserve is not mounted yet, so there is no destination filesystem to measure and no copy to render. What is outstanding is the activation itself, rendered below. The subdirectory step is deliberately absent from it: the copy recreates `records/` and `openbao/` on the mounted filesystem with their original ownership and modes in one operation, and their absence there is not something to remediate while a migration is open.",
    audit_reserve_finding_migration_mount_point: "{path} is a mount point at or under the holding directory, so no copy is rendered. The measurement, the copy and the verification have to cover exactly the same entries; a `mount --bind` of a directory on the same filesystem has the same device id, so `find -xdev` and `cp --one-file-system` both walk into it and the bind source\'s contents would be counted, copied into the reserve as if they were audit data, and duplicated on the host. Unmount it or move it out, then run this command again — the mount table is re-read on every pass.",
    audit_reserve_finding_migration_holding_kind: "{path} is {found}, not a directory, so nothing is measured, walked or copied from it and no copy is rendered. The copy this migration renders ends in `{path}/.`, and `cp` resolves that suffix however the path before it is spelled — so a symbolic link here is followed and whatever tree it names is copied into the reserve as audit data. The rendered type guard cannot catch it either: `find` neither descends a link named on its command line nor reports the link itself, so it exits zero over exactly the entry that must be refused. Deal with that path by hand, then run this command again. bootroot will not touch it.",
    audit_reserve_finding_migration_deferred: "This run also fails a check that would otherwise have stopped it before this outcome could be reported: {refusal} Migration incomplete is reported ahead of the image contract, the artifacts and the mount and overrides all of them, so that refusal is named here rather than raised — and while it stands no activation and no copy is rendered, because the reserve it describes cannot be made. Put the configuration back as it was and run this command again, or roll the migration back below.",
    audit_reserve_finding_migration_forbidden_entry: "{path} under the holding directory is {found}, so no copy is rendered. This migration carries only directories and regular files, which is stricter than what the store itself enforces and deliberately so: `cp -a` recreates a symbolic link pointing wherever it pointed before, which is the one entry that can put audit bytes outside the ceiling this reserve imposes, and a manifest recording a path and a link target as two variable-length fields cannot tell `a b` linked to `c` from `a` linked to `b c`. Nothing in this deployment creates one. Deal with that entry by hand, then run this command again.",
    audit_reserve_finding_migration_arithmetic: "{figure} cannot be represented as a 64-bit byte count, so the capacity check cannot be decided and no copy is rendered. A byte figure that does not fit fails closed rather than wrapping: a wrapped product is a fit check that turns \"does not fit\" into a pass.",
    audit_reserve_finding_migration_capacity_fits: "The mounted filesystem has {available} bytes available to an unprivileged writer, the holding directory\'s allocated size is {source} bytes, and the check requires a {margin}-byte margin above it for block rounding and per-directory metadata. The contents fit, so the copy is rendered below. The figure compared is the filesystem\'s available space, never `audit_store_reserve_bytes`: the ext4 filesystem inside the image holds less than the image does, its journal and metadata being real and already allocated.",
    audit_reserve_finding_migration_capacity_short: "The mounted filesystem has {available} bytes available to an unprivileged writer, the holding directory\'s allocated size is {source} bytes, and the check requires a {margin}-byte margin above it. The contents do not fit, so **no copy is rendered** — a copy that fails part-way leaves a partially populated store beside the holding directory. Two ways forward: raise `audit_store_reserve_bytes` and replace the image by the documented procedure in \"The shared audit store\" in the operations manual, or prune OpenBao\'s audit log under the holding directory first. The rollback below is the third option and undoes nothing you have copied that matters, the source never having been modified.",
    audit_reserve_note_migrated_reclaimable: "Reclaimable: {path} holds {size} bytes outside the reserve, a redundant second copy kept by the closing rename rather than deleted. Removing it is yours to do, and only after confirming nothing is mounted under it; bootroot renders no delete for it.",
    audit_reserve_note_migrated_reclaimable_unsized: "Reclaimable: {path} is a redundant second copy outside the reserve, kept by the closing rename rather than deleted. Its size could not be summed. Removing it is yours to do, and only after confirming nothing is mounted under it; bootroot renders no delete for it.",
    audit_reserve_note_retained_image_reclaimable: "Reclaimable: {path} holds {size} bytes, the image the reserve was replaced from. It is kept until this run reports enforced, so the replacement can be rolled back by renaming it into place; now that it has, removing it is yours to do. bootroot renders no delete for it.",
    audit_reserve_figure_source_plus_margin: "the allocated size of {path} plus the copy margin",
    audit_reserve_migration_prerequisites: "Prerequisites for the commands above: diffutils, for `cmp`, which a stripped host may lack; and coreutils 8.25 or newer, where `sha256sum` gained `--zero`. Every manifest is NUL-delimited and compared byte for byte, because every filename under `openbao/` is whatever that container wrote — a name holding a newline splits a line-delimited manifest into two records. Remove the `mktemp -d` scratch directory yourself afterwards; bootroot renders no delete.",
    audit_reserve_migration_window: "These commands assume one maintenance window: both writers stopped throughout, the steps run in the order given, no privileged mount change between this run and the command it rendered, and nothing under the holding directory changed between a check and the command it authorises. `find -xdev` excludes a different-device mount from the measurement, the copy and all three comparisons alike, so they cannot disagree over one; a same-device bind mount is caught only by the mount-table read above, which runs when bootroot renders and not while you copy. Under those conditions the three cover the same entries — not unconditionally.",
    audit_reserve_step_aside_rename: "Rename the store aside and recreate an empty mount point. This is what opens the migration: the holding directory keeps every byte where it is, and the empty directory left behind is what the reserve is mounted over.",
    audit_reserve_step_type_guard: "Run the type guard over the holding directory. It exits non-zero both when it finds an entry that is neither a directory nor a regular file and when the walk itself fails, so a long copy is not spent on a set that will be rejected. Go on only where it exits 0.",
    audit_reserve_step_copy: "Copy the holding directory\'s contents onto the mounted reserve. One whole-subtree operation: nothing is dereferenced, hard links stay shared, and the trailing `/.` is what recreates `records/` and `openbao/` with their original ownership and modes. Re-running it over a partial copy is how this step resumes; the source is never modified.",
    audit_reserve_step_verify: "Verify the copy. The type guard over both trees, then three comparisons — metadata, regular-file sizes, then content — each staged to its own file and ordered by its own `sort -z`. All four must exit zero; stop at the first that does not, and do not go on to the closing rename. No stage is a pipeline: a pipeline reports its last command\'s status, so a failed walk would read as a clean tree.",
    audit_reserve_step_closing_rename: "Close the migration with a rename, never a deletion, and only once the guard and all three comparisons have exited zero. It is a separate step and is never chained onto the copy or the verification.",
    audit_reserve_step_rollback: "Or roll back, which is offered only while the holding directory still carries its `.pre-mount` name. The partial copy on the mounted filesystem is discarded with the unmount, which is safe precisely because the source was never modified; the `rmdir` is non-recursive and removes the mount point only if the unmount left it empty. The `umount` applies only where the reserve is already mounted.",
};
