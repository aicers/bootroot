FROM golang:alpine AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN go build -o bootroot-agent ./cmd/agent

FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/bootroot-agent .

# Expose HTTP-01 challenge port
EXPOSE 80

ENTRYPOINT ["/app/bootroot-agent"]
